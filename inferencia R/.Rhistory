data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Resumen del test de Tukey
tukey_summary <- summary(tukey_results)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Resumen del test de Tukey
tukey_summary <- summary(tukey_results)
print(tukey_summary)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0.05)
marcas_iguales <- tukey_results$group[tukey_results$`p adj` > 0.05]
# Mostrar marcas con medias iguales
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "))
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0.05)
marcas_iguales <- tukey_results$group[tukey_results$`p adj` > 0]
# Mostrar marcas con medias iguales
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "))
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0.05)
marcas_iguales <- tukey_results$group[tukey_results$`p adj` > 0]
# Mostrar marcas con medias iguales
# Mostrar información sobre las marcas con medias iguales
if (length(marcas_iguales) == 0) {
cat("No hay marcas con medias de precios iguales.\n")
} else {
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "), "\n")
}
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- tukey_results$Make[tukey_results$`p adj` > 0]
# Mostrar marcas con medias iguales
# Mostrar información sobre las marcas con medias iguales
if (length(marcas_iguales) == 0) {
cat("No hay marcas con medias de precios iguales.\n")
} else {
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "), "\n")
}
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- levels(tukey_results$group)[tukey_results$`p adj` > 0]
# Mostrar información sobre las marcas con medias iguales
if (length(marcas_iguales) == 0) {
cat("No hay marcas con medias de precios iguales.\n")
} else {
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "), "\n")
}
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- levels(tukey_results$group)[tukey_results$p adj > 0]
View(tukey_results)
View(tukey_results)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- unique(as.vector(tukey_results[,"group"]))[tukey_results[,"p adj"] > 0]
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
print("Resultados del Test de Tukey:")
print(tukey_results)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- (as.vector(tukey_results[,"group"]))[tukey_results[,"p adj"] > 0]
View(modelo_anova)
View(tukey_results)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
# Convertir a data frame
tukey_results_df <- as.data.frame(tukey_results)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
# Obtener las comparaciones y los valores p ajustados
comparisons <- tukey_results$comparison
p_adj <- p.adjust(tukey_results$p.adj, method = "holm")
# Crear un data.frame con la información relevante
tukey_df <- data.frame(Comparison = comparisons, P_Adjusted = p_adj)
# Imprimir los resultados del Test de Tukey
cat("Resultados del Test de Tukey:\n")
print(tukey_df)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- tukey_df$Comparison[tukey_df$P_Adjusted > 0]
# Mostrar información sobre las marcas con medias iguales
if (length(marcas_iguales) == 0) {
cat("No hay marcas con medias de precios iguales.\n")
} else {
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "), "\n")
}
View(tukey_df)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
# Extraer comparaciones y p valores
comparisons <- as.data.frame(tukey_results$`group`)
p_vals <- tukey_results$`p adj`
# Crear un data.frame con la información relevante
tukey_df <- data.frame(Comparison = comparisons$grp1, Group2 = comparisons$grp2, P_Adjusted = p_vals)
# Imprimir los resultados del Test de Tukey
cat("Resultados del Test de Tukey:\n")
print(tukey_df)
# Obtener marcas con medias iguales (p adj > 0)
marcas_iguales <- unique(c(tukey_df$Comparison[tukey_df$P_Adjusted > 0], tukey_df$Group2[tukey_df$P_Adjusted > 0]))
# Mostrar información sobre las marcas con medias iguales
if (length(marcas_iguales) == 0) {
cat("No hay marcas con medias de precios iguales.\n")
} else {
cat("Marcas con medias de precios iguales:", paste(marcas_iguales, collapse = ", "), "\n")
}
install.packages('multcomp')
library(readr)
library(tidyverse)
library(dplyr)
library(xts)
library(psych)
library(gmodels)
library(MASS)
library(fitdistrplus)
library(lmtest)
library(fdth)
library(ggplot2)
library(plotly)
library(PASWR2)
library(lattice)
library(descr)
library(openxlsx)
library(kableExtra)
library(pastecs)
#Asignar directorio de trabajo
setwd('C:/Universidad/Universidad/Inferencia Estadistica/inferencia-estadistica-R/inferencia R')
#Comprobar directorio de trabajo
getwd()
datos <-  read_csv("datos_reducidos2.csv", show_col_types = FALSE)
#view(datos)
attach(datos)
data  <- datos %>%
filter(year >= 2018 & year <= 2020)
attach(data)
library(readr)
library(tidyverse)
library(dplyr)
library(xts)
library(psych)
library(gmodels)
library(MASS)
library(fitdistrplus)
library(lmtest)
library(fdth)
library(ggplot2)
library(plotly)
library(PASWR2)
library(lattice)
library(descr)
library(openxlsx)
library(kableExtra)
library(pastecs)
library(multcomp)
#Asignar directorio de trabajo
setwd('C:/Universidad/Universidad/Inferencia Estadistica/inferencia-estadistica-R/inferencia R')
#Comprobar directorio de trabajo
getwd()
datos <-  read_csv("datos_reducidos2.csv", show_col_types = FALSE)
#view(datos)
attach(datos)
data  <- datos %>%
filter(year >= 2018 & year <= 2020)
attach(data)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Supongamos que 'data' es tu conjunto de datos y 'Make' es la variable categórica que representa las marcas
# Instalar y cargar el paquete multcomp si no está instalado
if (!requireNamespace("multcomp", quietly = TRUE)) {
install.packages("multcomp")
}
library(multcomp)
# Realizar un análisis de varianza (ANOVA)
modelo_anova <- aov(price ~ Make, data = data)
# Crear un objeto glht para realizar las comparaciones
library(multcomp)
tukey_results <- glht(modelo_anova, linfct = mcp(Make = "Tukey"))
# Realizar ajuste de p valores
tukey_p_vals <- summary(tukey_results)$test$pvalues
# Crear un data.frame con la información relevante
tukey_df <- data.frame(Comparison = names(tukey_p_vals), P_Adjusted = tukey_p_vals)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
# Extraer comparaciones y p valores
comparisons <- as.data.frame(tukey_results$`group`)
p_vals <- tukey_results$`p adj`
# Crear un data.frame con la información relevante
tukey_df <- data.frame(Comparison = comparisons$grp1, Group2 = comparisons$grp2, P_Adjusted = p_vals)
# Imprimir los resultados del Test de Tukey
cat("Resultados del Test de Tukey:\n")
print(tukey_df)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
# Imprimir los resultados del Test de Tukey
cat("Resultados del Test de Tukey:\n")
print(tukey_results)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
library(readr)
library(tidyverse)
library(dplyr)
library(xts)
library(psych)
library(gmodels)
library(MASS)
library(fitdistrplus)
library(lmtest)
library(fdth)
library(ggplot2)
library(plotly)
library(PASWR2)
library(lattice)
library(descr)
library(openxlsx)
library(kableExtra)
library(pastecs)
library(multcomp)
#Asignar directorio de trabajo
setwd('C:/Universidad/Universidad/Inferencia Estadistica/inferencia-estadistica-R/inferencia R')
#Comprobar directorio de trabajo
getwd()
datos <-  read_csv("datos_reducidos2.csv", show_col_types = FALSE)
#view(datos)
attach(datos)
data  <- datos %>%
filter(year >= 2018 & year <= 2020)
attach(data)
knitr::opts_chunk$set(echo = TRUE, warning = F, message = F)
## Hipótesis
#- \(H_0\): \(\mu_{\text{audi}} = \mu_{\text{BMW}} = \mu_{\text{Ford}} = \mu_{\text{Hyundai}} = \mu_{\text{skoda}} = \mu_{\text{toyota}} = \mu_{\text{vw}}\), es decir, no hay diferencias significativas en los precios promedio entre las marcas de autos.
#- \(H_1\): Al menos dos de las medias poblacionales son diferentes, es decir, al menos una de las marcas tiene un precio promedio significativamente diferente.
# Convertir 'Make' a factor
data$Make <- as.factor(data$Make)
# Boxplot
boxplot(price ~ Make, data = data, main = "Boxplot de Precio por Marca", xlab = "Marca", ylab = "Precio")
# Promedio por marca
mean_by_make <- tapply(data$price, data$Make, mean)
print("Promedio de precio por marca:")
print(mean_by_make)
# ANOVA
modelo_anova <- aov(lm(price ~ Make, data = data))
summary(modelo_anova)
# Test de Tukey
tukey_results <- TukeyHSD(modelo_anova)
# Imprimir los resultados del Test de Tukey
cat("Resultados del Test de Tukey:\n")
print(tukey_results)
View(tukey_results)
tukey_results[["Make"]]
View(tukey_results)
#Ho: Bo = 0
#Hi: Bo != 0
options(scipen = 999)
# Crear un modelo de regresión lineal
modelo <- lm(price ~ mileage, data = data)
# Imprimir un resumen del modelo
summary(modelo)
intercepto <- coef(modelo)[1]
pendiente <- coef(modelo)[2]
# Determinar el signo del coeficiente de la pendiente
signo <- ifelse(pendiente >= 0, "+", "-")
# Imprimir la ecuación de la recta
pendiente <- paste("Ecuación de la recta: Precio =", round(intercepto, 2), signo, abs(round(pendiente, 2)), "* Kilometraje")
# Graficar los datos
plot(data$mileage, data$price, main = pendiente, xlab = "Kilometraje", ylab = "Precio", pch = 1, col = "blue")
# Agregar la línea de regresión al gráfico
abline(modelo, col = "red")
